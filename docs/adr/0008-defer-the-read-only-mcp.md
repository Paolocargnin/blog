# Defer a read-only MCP until the corpus justifies it

The blog may later provide a local MCP that exposes published Posts by default and reads the private Workspace only through explicit startup opt-in. It will expose no write tools and will not be remotely deployed. Implementation is deferred until the blog has more than ten published Posts, when structured AI retrieval provides clearer value than the website and RSS feed alone.

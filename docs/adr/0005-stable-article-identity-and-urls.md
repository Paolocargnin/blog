# Separate stable article identity from its public URL

Every Draft receives a generated identifier that remains stable through review, publication, and later corrections. Its human-readable slug may change before publication, but becomes the permanent public URL when published; later slug changes require a redirect. This preserves references across workflow stages while keeping readable URLs.

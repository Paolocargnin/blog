# Keep Post metadata minimal and non-duplicative

Published Posts require `id`, `title`, `description`, `publishedAt`, and controlled `tags`. `updatedAt` and dated correction summaries appear only when applicable. The filename is the public slug; Author and language are site-wide invariants; lifecycle state comes from directory placement. Workspace articles may defer description and tags until they become publication candidates.

# Use a controlled tag vocabulary

Posts reference stable lowercase tag identifiers whose display labels are defined centrally. Introducing a tag is explicit, and public posts normally use one to four tags. This prevents synonymous or differently cased labels from fragmenting archives while allowing the site to generate one canonical page per tag.

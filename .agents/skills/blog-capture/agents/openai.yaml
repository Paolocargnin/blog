interface:
  display_name: "Blog Capture"
  short_description: "Capture a private atomic Note"
  default_prompt: "Use $blog-capture to capture a new private Note for the blog."
policy:
  allow_implicit_invocation: false
